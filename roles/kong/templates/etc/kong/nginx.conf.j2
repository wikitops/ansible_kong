# {{ ansible _manage }}

worker_processes auto;
daemon on;

pid pids/nginx.pid;
error_log logs/error.log {{ kong_log_level }};

worker_rlimit_nofile {{ kong_max_files_limit }};

events {
    worker_connections 16384;
    multi_accept on;
}

http {
    include 'nginx-kong.conf';
}
